# Client Configuration
#
# PRODUCTION NOTE: In production, these URLs must use secure protocols:
#   - NEXT_PUBLIC_SERVER_URL must use https:// (e.g., https://api.example.com)
#   - NEXT_PUBLIC_WS_URL must use wss:// (e.g., wss://api.example.com)
# Using http:// or ws:// in production will expose traffic to interception.
#
NEXT_PUBLIC_SERVER_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Error Tracking (Sentry)
# Get your DSN from https://sentry.io → Project Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=
